import CardListStaggered from '../../components/CardListStaggered';

function CardListStaggeredExp() {
  return (
    <div className="flex-1 flex flex-col h-full items-center justify-center bg-zinc-950 min-h-[calc(100vh-64px)]">
      <CardListStaggered />
    </div>
  );
}

export default CardListStaggeredExp;
